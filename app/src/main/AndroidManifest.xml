<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="soumu.seed.android41">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DialogActivity" />
        <activity android:name=".TouchActivity" />
        <activity android:name=".BarcodeActivity" />
        <activity android:name=".CaptureAnyOrientationActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".AsyncActivity" />
        <activity android:name=".UnitechActivity" />
        <activity android:name=".UriageActivity" />
        <activity android:name=".LifecycleActivity"></activity>
    </application>

</manifest>
